//Create function to retrieve saved books from database and store them in a var

//When books are pulled, populate them into cards and append them to the container.

//Add event listener to the delete button. When clicked, delete from collection
